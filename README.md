Ansible scaffold for deploying the Spring Petclinic app.

Structure created from spring-app-ansible-plan.md. Use `site.yml` to run plays.

Run example:

ansible-playbook -i inventory/hosts.yml site.yml --ask-become-pass
